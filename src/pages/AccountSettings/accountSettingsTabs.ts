import { routes } from '@/models/router/routes';
import { UserIcon } from 'lucide-react';

export const tabs = [
  {
    id: routes.accountSettingsAccount,
    label: 'Account',
    icon: UserIcon,
  },
];
